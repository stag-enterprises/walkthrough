bind alt-comma 'walkthrough -b >/dev/null'
bind alt-.     'walkthrough -n >/dev/null'
bind alt-/     'walkthrough -l' repaint
bind alt-\;    'walkthrough -a >/dev/null'
bind alt-\'    'walkthrough -s >/dev/null'

